import { CanActivateFn } from '@angular/router';

/**
 * Starter route guard placeholder.
 * Replace `true` with real auth/permission checks when auth is added.
 */
export const authGuard: CanActivateFn = () => true;
